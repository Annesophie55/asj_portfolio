<div class="btn_bloc">
    <button>Modifier</button>
    <button>Archiver</button>
    <form method="post" action="{{ path('app_experience_delete', {'id': data.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.id) }}">
        <button class="btn">Delete</button>
    </form>
</div>